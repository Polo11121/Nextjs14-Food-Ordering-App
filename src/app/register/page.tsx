import { AuthenticationForm } from "@/components/ui";

const RegisterPage = () => (
  <section className="h-container">
    <h1 className="text-center text-primary text-4xl">Register</h1>
    <AuthenticationForm />
  </section>
);

export default RegisterPage;
